/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ModulecontainerComponent } from './modulecontainer.component';

describe('Component: Modulecontainer', () => {
  it('should create an instance', () => {
    let component = new ModulecontainerComponent();
    expect(component).toBeTruthy();
  });
});
